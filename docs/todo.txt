
incoming files is currently
{
  'hiker.c' => {
    'content' => '#include',
    'truncated' => false
  }
}
Drop the truncation and just send in the content directly.
As per the current README.md file.
-------------------------------------------------
Possibly make max_file_size as incoming parameter?
-------------------------------------------------
