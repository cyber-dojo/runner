
Add extra (non /sandbox) files to the tar-file.
Specifically the create_text_file_tar_list.sh script
This could go into a --tmpfs mounted dir.

Note: I cannot inject the /usr/local/bin/red_amber_green.rb
since this is specific to each image.
